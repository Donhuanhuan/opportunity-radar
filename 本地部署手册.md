# Make monney · 本地部署手册（路径 B：半本机 agent-browser）

> 适用：Windows 10/11 + Git Bash。阅读时间 ≈ 4 分钟，执行时间 ≈ 8 分钟。

---

## 0. 现状（已经做完，不用动）

| 项 | 状态 |
|---|---|
| DeepSeek API key 已从桌面文件读取并写入 `.env` | ✅ |
| 4 个核心依赖（requests/feedparser/python-dotenv/PyYAML）已装到 managed Python | ✅ |
| 飞书 App + 商机池表（8 字段）+ 内容草稿表（17 字段）已建 | ✅ |
| M3 本地发布器 + 真人节奏 + 审核闸 + 3 Agent 编排已 push | ✅ |
| 已新建一键脚本 `scripts/local_run.sh`（支持 scan/m2/m3/audit-init/env-check） | ✅ |

---

## 1. 你要做的（5 步，约 8 分钟）

### Step 1 · 验证环境（10 秒）

在 Git Bash 里跑：

```bash
cd "/d/WorkBuddy/Make monney/Make monney/opportunity-radar"
./scripts/local_run.sh env-check
```

应该看到 6 个 ✅。如果有 ⚠️，告诉我哪个。

---

### Step 2 · 让 agent-browser 拿到登录态（关键中的关键）

⚠️ **重要：你在 Edge/Chrome 登录小红书和知乎 ≠ agent-browser 登录了**

agent-browser 跑的是 **云端 daemon 的 Chromium 浏览器**，跟你电脑上的 Edge/Chrome 是**两个独立的浏览器进程**。Cookie 不互通，登录态不共享。

你要做的是：**在 agent-browser 打开的浏览器里重新登录一次**。

等我说"我现在打开小红书后台"时，会跳出一个网页（daemon 里的浏览器），你**在那个页面里**扫码登录（30 秒）。

---

### Step 3 · 初始化「内容审核」表（一键）

审核表用于你一键过/批/驳草稿：

```bash
./scripts/local_run.sh audit-init
```

我会拿到一个新 `table_id`，把它写进 `.env` 的 `FEISHU_AUDIT_TABLE_ID`。

---

### Step 4 · 跑一次 M2 内容工厂（生成第一份草稿）

```bash
./scripts/local_run.sh m2
```

输出：
- 飞书「内容草稿」表里多 1-3 条记录
- `data/drafts.jsonl` 本地落盘
- 飞书群收到卡片「📝 3 条新草稿待审核」

---

### Step 5 · 你到飞书勾一下，然后跑 M3 发布

1. 打开飞书「内容草稿」表 → 把「状态」字段改成 **已通过**（先选 1 条体验）
2. 跑：

```bash
./scripts/local_run.sh m3
```

发布器会用 agent-browser 把那条草稿发到小红书和知乎创作者后台。

---

## 2. agent-browser 是啥？为啥要装？

**好问题——不用装。**

agent-browser 是 WorkBuddy 内置的 **云端 Chromium daemon**，已经在我（助手）这边常驻。你**不需要装任何东西**，我发"agent-browser open creator.xiaohongshu.com"指令时，会自动弹出窗口。

唯一要做的：在那个弹出的窗口里**手动登录一次**（30 秒）。之后 daemon 会持久化 cookie。

---

## 3. 关键决策点（请按你习惯单字母回）

我需要你回答 1 个事，否则下一步我没法推进：

### Q1 · 登录态怎么办？（三选一）

| 选项 | 操作 | 风控 | 推荐 |
|---|---|---|---|
| **A · 在 agent-browser 弹出页里重新登录** | 我发指令 → 弹窗 → 你扫码登录（30 秒/平台） | 🟡 中 | ✅ 推荐，最快 |
| **B · 我帮你导 Cookie 注入 daemon** | 你在 Edge 导出小红书/知乎 Cookie 文件 → 给我 | 🟢 低 | 适合你 Edge 里已有长期登录态 |
| **C · 走路径 C（本机 Playwright 服务）** | 你本机装 30 行 Node 服务 + 我 webhook 推任务 | 🟢 低 | 风险最低，但你要额外装服务 |

> 我的建议：**选 A**。最快、零额外安装、风控可接受（daemon 的 IP 是固定的，等同"老用户老 IP"）。

---

## 4. 今天剩下的时间表

| 时间 | 我做 | 你做 |
|---|---|---|
| 15:50 | 等你回 Q1 | 选 A/B/C |
| 15:55 | `audit-init` 建审核表 | 等回写新 table_id |
| 16:00 | 跑 M2 生成第一份草稿 | 等飞书通知 → 勾一条 |
| 16:05 | 跑 M3 发布 | 看发布效果 |
| 16:30 | 改 `.env` 开关 → 准备明天 9:00 自动跑 | — |

---

## 5. 明天 9:00 自动跑会做这些

```
09:00 GitHub Actions 触发
  → M1 抓 5 源（HN/PH/Reddit/Google Trends/微博热搜）
  → 写商机池（8字段）
  → M2 拉 Top 3 → DeepSeek 分析 → 生成双版本文案
  → 写内容草稿表 + data/drafts.jsonl
  → 飞书群推送"3 条新草稿待审核"
09:10 你醒来：打开飞书，勾 3 条草稿 → 等我 M3 自动发（你不在场也照发）
```

如果你不勾 → 当天 M3 不发，安全第一。
